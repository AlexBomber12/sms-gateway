# PR-00: agent workflow foundation

## Goal
- Establish a deterministic, agent-driven workflow foundation for future PR tasks.

## Scope
- Add agent workflow rules in `AGENTS.md`.
- Add task scaffolding under `tasks/`.
- Add `scripts/run_tests.sh` with artefacts logging, retention, and latest pointer handling.
- Update `.gitignore`, `Makefile`, and add `.editorconfig`.

## Out of scope
- Changing gateway runtime behavior.
- Adding or modifying application features outside workflow tooling.

## Files to touch
- `AGENTS.md`
- `tasks/README.md`
- `tasks/PR-TEMPLATE.md`
- `tasks/PR-00.md`
- `scripts/run_tests.sh`
- `.gitignore`
- `Makefile`
- `.editorconfig`

## Steps
- [ ] Add deterministic agent workflow rules and task templates.
- [ ] Implement `scripts/run_tests.sh` with artefacts logging, retention, and CI_MODE export.
- [ ] Update ignore rules and test target.
- [ ] Run `bash scripts/run_tests.sh` and ensure green.

## Acceptance criteria
- `bash scripts/run_tests.sh` produces `artefacts/run-*` with logs and summary, updates `artefacts/latest`, and exits 0 on success.
- Task scaffolding files exist and follow the template structure.
- Agent rules are explicit and actionable.

## Test command
- `bash scripts/run_tests.sh`

# Example environment configuration for SMS Gateway
# Precedence:
# - GAMMU_DEBUGLEVEL overrides numeric LOGLEVEL for gammu-smsd debug output.
# - USB_VID/USB_PID override USB detection; otherwise MODEM_PORT, then lsusb.
SMSGW_VERSION=v1.0.0              # Required
MODEM_PORT=/dev/ttyUSB0           # Required - device path of your modem
TELEGRAM_BOT_TOKEN=your_token     # Required - Telegram bot token
TELEGRAM_CHAT_ID=your_chat_id     # Required - Telegram chat ID
LOGLEVEL=INFO                     # Optional - Python logging level (INFO or DEBUG)
GAMMU_DEBUGLEVEL=                 # Optional - numeric gammu-smsd debuglevel
DELIVERY_MODE=direct              # Optional - direct (default) or queue
GAMMU_SPOOL_PATH=/var/spool/gammu # Optional - path for Gammu spool directories
USB_VID=                          # Optional - USB vendor ID for modem reset
USB_PID=                          # Optional - USB product ID for modem reset
MODEM_TIMEOUT_THRESHOLD=3         # Optional - watchdog: timeouts before reset
RESET_MIN_INTERVAL=60             # Optional - watchdog: min seconds between resets
RESET_BACKOFF_STEP=30             # Optional - watchdog: backoff increment seconds
RESET_BACKOFF_MAX=300             # Optional - watchdog: max backoff seconds
RESET_BACKOFF_WINDOW=300          # Optional - watchdog: window for backoff accumulation
RESET_SETTLE_SECONDS=20           # Optional - watchdog: wait after USB reset
